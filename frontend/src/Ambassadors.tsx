// src/Ambassadors.tsx
import React from 'react';

interface AmbProps {
  contextPath: string;
}

const Ambassadors: React.FC<AmbProps> = ({ contextPath }) => {
  return (
    <div>
      <h1>Ambassadors Administration</h1>

    </div>
  );
};

export default Ambassadors;
